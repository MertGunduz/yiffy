TODO 
{
    In some countries e621 is banned.

    Do a ping check before, if you can't send ping. Then you are in prohibited access zone to E621 like Turkey.

    If it happens then give users a message that says it might be banned in your country, please use a vpn or proxy to access.

    ```
    It should be as a whole function that returns bool.
    Send 5 pings to the e621/e926. If the pings are successful, return a true. If it's not working return a false.  

    Function Name: apiConnect();
    ```
}